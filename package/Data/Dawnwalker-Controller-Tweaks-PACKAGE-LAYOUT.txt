Dawnwalker Controller Tweaks package layout

Use Vortex's Root (game folder) installer. The only runtime payload is one IoStore
container triple under Dawnwalker/Content/Paks/~mods/ at archive root.
Data/ retains this uniquely named layout note; no runtime files are duplicated.
mod.manifest and README.txt are at archive root. No Lua scripts are included.
Manage installation, deployment, conflicts and removal through Vortex.
